export default function ProjectWithdrawFlow({ projectId }: ProjectWithdrawFlowProps) {
  console.log('projectId', projectId);
  return <div>ProjectWithdrawFlow</div>;
}

interface ProjectWithdrawFlowProps {
  projectId: string;
}
