import React from 'react'

export default function ClientProfilePage(props: PageProps) {

// check if the user is client -> else redirect to home page


  return (
    <div>
        Flextern Client Profile Page
    </div>
  )
}

type PageProps = {
}